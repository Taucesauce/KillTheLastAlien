using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum FoodColor {
    Green = 0,
    Orange = 1,
    Pink = 2
}

public enum GameState {
    Menu,
    PlayerSelect,
    RoundActive,
    EndRound,
    EndGame
}