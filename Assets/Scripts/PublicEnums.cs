using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum FoodColor {
    Green,
    Orange,
    Pink
}

public enum GameState {
    Menu,
    Decision,
    Scramble,
    EndRound,
    EndGame
}